# RESULTS.md

**规则（§8）**：本文件只写**通过 §5–§7 的结果**。

---

## 可测性（v2.2 §6.7）—— 关于**仪器**的结果，不是关于生物学的结果

本节报告的是"这套数据能测出多大的效应"，属仪器性质，不受 §2 门顺序封锁；生物学结论仍被封锁（见下节）。

### MDPC（α = 0.05 双侧，功效 80%；`GSE154826`，19 对配对供者）

| 基因 | 基线（normal） | SE(diff) 配对 | **MDPC@80%** | 〔v2.1 旧 1-SE 口径〕 |
|---|---|---|---|---|
| CCL3 | 0.4953 | 4.43 pp | **25.0%** | 8.9% |
| CCL4 | 0.7750 | 5.22 pp | **18.9%** | 6.7% |
| CCL5 | 0.7408 | 6.15 pp | **23.3%** | 8.3% |
| **XCL1** | **0.0930** | 2.23 pp | **67.1%** | 23.9% |

对照基准 = R1 自身观测到的比例降幅中位数 **−23.6%**。
按 §6.7 规则 3，只有 CCL4 与 CCL5 的 MDPC 低于该基准；**XCL1 的 MDPC 是基准的近三倍**。
补注：CCL3 的 MDPC 高于基准但它自身被检出下降（−27.1%，p = 0.0072）——规则 3 只约束"保留/不变"型陈述，不与之矛盾。

### 组内相关与衰减校正（§6.7 规则 5）

| 基因 | 观测 r | 技术 SD / 供者间 SD | 可靠度 | 衰减校正后 r |
|---|---|---|---|---|
| CCL3 | +0.177 | 0.217 – 0.389 | 0.85 – 0.95 | +0.19 ~ +0.21 |
| CCL4 | −0.071 | 0.131 – 0.140 | 0.98 | −0.07 |
| CCL5 | −0.046 | 0.150 – 0.242 | 0.94 – 0.98 | −0.05 |
| XCL1 | −0.089 | 0.333 – 0.366 | 0.87 – 0.89 | −0.10 |

**可靠度 0.85–0.98，近零相关经校正后仍近零** → 配对在本设计上确实不省方差，这是设计事实而非测量误差。限制：技术项仅来自一位供者（patient 522）。

### 尺度可行性

检出与 R1 同比例（−23.6%）的变化所需配对供者数：CCL3 **21**、CCL4 **12**（已足）、CCL5 **18**（已足）、**XCL1 153**。现有 n = 19。

> **XCL1 的分离在检出率尺度、在公共 scRNA-seq 数据里不可及。** 根因是基线 0.09 太低，不是 n 不够。

### ⚠ 防误读：跨器官核查的 `CCL3`–`CCL5` 「无差异」不推翻本节

独立任务「跨器官 NK 趋化因子可测性核查」（`ORGAN_TABLE_v2.md`）在 Domínguez Conde 2022 图谱上测得 `CCL3`／`CCL4`／`CCL5` 的跨器官离散度自助法区间下界全部 < 1.2×，即**该数据集不支持「器官间检出率有差异」**。这与本节 `GSE154826` 上的 `CCL3 −27.1%`（p = 0.0072）、`CCL4 −23.6%`（p = 0.0025）、`CCL5 −23.6%`（p = 0.0108）**不矛盾**：

- **配对设计效力更高**：本节 **19 对**配对供者，对比在个体内定义（同一人的肿瘤 vs 邻近正常）；跨器官核查每器官仅 **3–6 名**供者，且未配对。
- **须写准的一点**：该效力优势来自样本量与对比的定义方式，**不来自配对相关性抵消方差**——本节上方已实测配对相关近零（r = −0.089 ~ +0.177），「配对在本设计上确实不省方差」。
- **两者估计的不是同一个量**：本节问同一器官内的疾病状态对比，跨器官核查问健康状态下的器官身份对比。

详见 `CLAIMS_ORGAN.md` I5–I8。

---

## 本轮通过 §5–§7 的**生物学**结果：**无（空集）**

这是 §0 预先声明为有效的那类产出：

> **空结果预先声明为有效结果。** ……那就是本轮的产出，写清楚即可，不要为了有东西交而降低判据。

本轮**没有任何**关于"NK 或 CD8 T 的调节/杀伤模块比值在绿→红对比下如何变化"的数值通过判据。

逐条说明卡在哪一条，见 [`EXCLUDED.md`](EXCLUDED.md)。

---

## 为什么这不是"没做出来"

本轮**做出了三件确定的事**，它们只是不属于"§5–§7 通过的比较结果"这一类，所以不放在本文件里：

1. **Kersten 2022 的极性来源已钉死**，且按 A1.0 的预写规则触发降级 → `CLAIMS.md` §A（K1–K7）。
2. **A1.4 已执行完毕并落在预写的第三分支 `UNMEASURED`**，且是超定的（四条独立理由）→ `CLAIMS.md` §B（P1–P8）。
3. **三条推翻规范自身初判的实测发现** → `CLAIMS.md` §C（N1、N2、N4）：
   - `N1` v1.0 §6 的"调节模块"在 NK 内部不同向（Xcl1 与 Ccl3/4/5 系统性反向），模块均值在对冲；
   - `N2` §5 之后两个谱系不共享基因集，"在 CD8T 上跑同一计算"字面不成立——这比 A1.1 原来的论证更强，且**不依赖极性**；
   - `N4` 绿→红几乎是亚群构成的整体更换（CD8T-3 从 45.3% → 94.2%），§7 的 between 项将占主导。

第 3 条里的 N1 与 N2 是本轮真正的产出：**它们改的是工具，不是结论。**

---

## 待判定项（等确认后才知道要不要算）

| 项 | 阻塞在 |
|---|---|
| NK 与 CD8T 模块比值的正式对比（§6 主计算） | 交接语明令停下；且需先解决 N1（模块定义）与数据集 n 的问题 |
| Kitagawa 三项分解（§7） | 同上；N4 已预警 between 占主导 |
| Q4 是否已被占位（G-KILL-2） | 交接语明令停下 |
