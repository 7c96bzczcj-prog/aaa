# CLAIMS_S3.md —— 任务 S-3 的断言登记

> ## ⚠ 贯穿全表的限制（§7，置于开头）
> **Kaede 的绿／红对比无法区分「清除」与「重编程」。** 本表所有 f 值回答的是「**若为清除，需要多大比例**」，**不是**「实际清除了多少」。任何把 f 读作实际清除率的用法都超出本设计。

等级：**[M]** 本轮实测 ｜ **[V]** 原始文献核实 ｜ **[L]** 模型知识 ｜ **[X]** 已被推翻

---

## A. 数据可得性（**先于一切计算**）

| # | 断言 | 等级 | 出处 | 是否推翻 Claude 初判 |
|---|---|---|---|---|
| **A1** | **`GSE221064` 不在盘上，本轮未取数。** 交接语称其为「已有的」数据，核查为否：`data/raw/` 无任何相关文件，`DATASETS.md` 记 `NOT_RUN`（仅 filtered matrix → 判据 (a) 不通过；原始 FASTQ 在 `PRJNA912695`）。**§2–§4 全部在 `E-MTAB-10176` 上执行**，这不是替代选择，是唯一可执行的选择 | **[M]** | `data/raw/` 目录；`DATASETS.md` | —— 规范前提与实况不符，**请裁定是否取数** |
| **A2** | **`E-MTAB-10176` 中颜色与文库完全共线。** 四个文库 `MH1_G24`／`MH2_R24`／`MH3_G72`／`MH4_R72`，每个颜色×时点恰好一个文库；`sdrf` 显示绿与红被**分选进彼此分开的文库**（门 `CD45+ Ter119− CD11b−`），每样本 4 条泳道为**技术重复**而非生物学重复 | **[M]** | `data/raw/E-MTAB-10176.sdrf.txt` | 否 —— 与既有 `EXCLUDED.md` 判定一致 |
| **A3** | **因此 §2.4 的两项要求在本数据集上不可按原文执行**：(i)「信噪比门槛」需同色复孔估噪声底，本数据集**无同色复孔**；既有噪声锚（`Xcl1` ±15.5、`Ccl4` ±11.3、`Ccl5` ±6.6 pp）来自 `GSE221064` 的绿-绿复孔，A1.3c 禁止迁移。(ii)「按文库重抽，三个文库」不可执行——重抽文库等于重抽颜色；且「三个文库」这一措辞本身指向 `GSE221064`（绿 ×2 + 红 ×1） | **[M]** | `CLEARANCE_BOUNDS.md` §0.2 | —— **规范与实况不符，非执行放宽** |
| **A4** | **替代做法已声明**：门槛改用项目自身的可测性带（两侧 ≥ 5% 且较大侧阳性细胞 ≥ 10）；区间改为颜色×时点内**重抽细胞**（2000 次），**该区间只含抽样不确定度，是真实不确定度的下界** | **[M]** | 同上 | 否 |

---

## B. §2.3 粗算的复核（**Claude 初判被推翻的部分**）

| # | 断言 | 等级 | 出处 | 是否推翻 Claude 初判 |
|---|---|---|---|---|
| **B1** | **七个基因的升降方向全部复现。** `Ccl3`／`Ccl4`／`Ccl5` 降，`Itga1`／`Cd160`／`Ldha`／`Xcl1` 升 | **[M]** | `results/s3_ppt_recheck.csv` | 否 |
| **B2** | **f 值有五个实质性移动。** `Ccl4` 0.291 → **0.596**（+0.304）；`Ccl3` 0.101 → **0.337**（+0.237）；`Itga1` 0.547 → **0.716**；`Cd160` 0.549 → **0.389**；`Ldha` 0.156 → 0.072；`Xcl1` 0.072 → 0.140。仅 `Ccl5` 几乎不动（0.852 → 0.853） | **[M]** | 同上 | **是** |
| **B3** | **§2.3 的核心论据不成立：`Itga1` 与 `Cd160` 不收敛。** §2.3 中两者相差 0.002（0.547／0.549），实测相差 **0.33**（0.716／0.389）。那份「近乎重合」是那组输入数值的性质，不是本数据集的性质 | **[M]** | 同上 | **是 —— 这是本轮对 Claude 粗算的主要推翻** |
| **B4** | **更强的一层：这两个基因都被 §2.4 自己的规则排除。** 实测 `Itga1` 在 CD8 T 中同向上升 16.5 pp（NK 20.9 pp）；`Cd160` 在 CD8 T 中同向上升 7.2 pp，**比 NK 的 6.3 pp 还多**。二者均满足「与 CD8 T 同步上升」→ 按 §2.4 不得用于估计 f | **[M]** | `results/s3_clearance_per_gene.csv` | **是** |
| **B5** | **`Cd160` 的方向在两个时点间翻转**：24 h 为降（绿 11.3% > 红 9.2%），72 h 为升（绿 9.4% < 红 20.0%）。合并 f_est = 0.389 是两个相反结果的平均，不是稳定量 | **[M]** | 同上 | **是** |
| **B6** | **`Xcl1` 的绝对水平与 §2.3 相差甚远**（§2.3 绿 76.02／红 81.95；实测 49.35／57.38）。方向一致 | **[M]** | `results/s3_ppt_recheck.csv` | **是**（数值层面） |
| **B7** | **「T差」的定义在 §2.3 中不自洽，须裁定。** 本文件明确定义 T差 = (绿−红)_NK − (绿−红)_CD8T（百分点），实测 `Ccl4` **+55.8**、`Ccl3` **+38.0**、`Ccl5` **+37.6**，与 §2.3 的 −0.03／0.87／25.58 全不相符。在该定义下 §2.3 对 `Ccl4` 的注解自相矛盾：T差 ≈ 0 意味着两类细胞变化幅度相同，即**完全趋同**，而注解写「几乎零趋同」 | **[M]** + 条文分析 | `CLEARANCE_BOUNDS.md` §1.2 | —— **规范内部不自洽，请裁定定义** |

---

## C. §2 的全基因扫描与判读

| # | 断言 | 等级 | 出处 | 是否推翻 Claude 初判 |
|---|---|---|---|---|
| **C1** | **门槛通过 6,753 / 16,227 个基因**（两侧 ≥ 5% 检出且较大侧阳性 ≥ 10）。下降侧 4,713、上升侧可用 1,261、上升侧因与 CD8 T 同步而排除 **779** | **[M]** | `results/s3_clearance_per_gene.csv` | 否 |
| **C2** | **f_est 的集中位置为 0.13–0.18。** 主口径（方向两时点稳定，n = 781）中位数 **0.179**，四分位距 0.167，10–90% [0.058, 0.367]。三种口径彼此接近 | **[M]** | `CLEARANCE_BOUNDS.md` §3.1 | 否 |
| **C3** | **判读落在 §2.5 预写表第二行**：f_est 集中但低于某些 f_min → 差额必须由状态改变承担 | 推论（按预写表） | `results/s3_verdict.txt` | 否 |
| **C4** | **募集模块恰是清除解释力最弱之处。** 三个募集因子的 f_min 全部高于 f_est 中位数：`Ccl3` 0.337（1.9×）、`Ccl4` 0.596（3.3×）、`Ccl5` **0.853（4.8×）**。取 f = 0.179 且被清除细胞全为阳性（最有利极端），可解释的下降份额仅 `Ccl5` **3.7%**、`Ccl4` **14.8%**、`Ccl3` **42.7%** | **[M]** | `CLEARANCE_BOUNDS.md` §4 | 否 |
| **C5** | **C3 的「集中／发散」门槛是我的选择，不是预注册量。** §2.5 未给数值。本轮采用四分位距 > 0.25 判发散；实测 0.167–0.173 故判「集中」。**若门槛取 0.15 则判发散，判读改落第三行**（本路径无效）。**请裁定门槛，或改为不设门槛只报分布** | 条文缺口 | `CLEARANCE_BOUNDS.md` §4.1 | —— **规范未写死，执行处补的，须回填** |
| **C6** | **C4 不依赖 C5 的门槛。** 无论判集中或发散，f_est 分布的 10–90% 上端 **0.367** 仍低于 `Ccl4` 的 0.596 与 `Ccl5` 的 0.853 | **[M]** | 同上 | 否 |

---

## D. §3 周转率比对

| # | 断言 | 等级 | 出处 | 是否推翻 Claude 初判 |
|---|---|---|---|---|
| **D1** | **绿∶红文库细胞数之比不能用作周转率。** 绿与红被分选进分开的文库，该比值只反映每个文库上机了多少细胞。本文件未据此计算任何周转率 | **[M]** | `E-MTAB-10176.sdrf.txt`；`TURNOVER_CHECK.md` §1.1 | —— 排除一条看似可用的路径 |
| **D2** | **原文确实报告了 NK 的 Kaede 绿／红数据**：Fig. 1 C／D 与 Fig. S1 B／C，流式，NK 门 `CD11b^−/lo CD11c⁻ CD3⁻ CD4⁻ CD8⁻ NK1.1⁺`，**n = 9（24 h）／5（72 h）／3（120 h）** 只小鼠 | **[V]** | JEM 2022，PMC9048291 全文 XML | 否 |
| **D3** | **原文从未测定死亡率。** 全文 `apoptos` 0 次、`viabilit` 0 次、`Annexin` 0 次、`caspase` 0 次、`death`／`dying` 0 次、`turnover` 0 次。记 **`NOT_REPORTED`** | **[V]** | 同上 | 否 —— §3 预期之内 |
| **D4** | **NK 特异的绿⁺／红⁺ 绝对数值记 `NOT_EXTRACTABLE`**：只存在于 Fig. S1 C 的图形中，正文与图注均未给数字。**未从图形目测取数** | **[V]** | 同上 | 否 |
| **D5** | **原文的关键定性结论**：「The total number of Kaede Green⁺ TILs **increased 10-fold** over the 5-d timeframe… In contrast, **the total number of Kaede Red⁺ cells appeared relatively constant**」 | **[V]** | 同上 | 否 |
| **D6** | **红池是只减不增的封闭队列**：光转换后新进入的细胞是绿的、不会变红，故红池只能因死亡或迁出而变小。**一个只减不增的队列其绝对数在 96 h 内「大致恒定」，意味着该窗口内净损失很小** | 推论（**[V]** 前提 + 设计逻辑） | `TURNOVER_CHECK.md` §3.1 | 否 |
| **D7** | **§3 的预写落点触发：纯清除解释与原文报告的红池动力学不相容。** §2 要求 f ≥ 0.34／0.60/0.85，上升侧只给出 0.13–0.18，原文报告红池绝对数大致恒定——三者无一支持 f ≥ 0.6 这一量级 | 推论 | `TURNOVER_CHECK.md` §4 | 否 —— 按 §3 预写，这是有价值的结论 |
| **D8** | **D7 的证据等级须写准。** 它建立在一句**定性**的、**关于 TILs 整体**的原文表述之上（NK 特异曲线未取数），且「净损失」含迁出与死亡两项。**足以否定「纯清除」这一强主张，不足以给出实际清除率的点值。** NK 样本量 n = 9/5/3 亦极小 | 限制声明 | `TURNOVER_CHECK.md` §3.3 | —— 主动降级自己的结论 |

---

## E. §4 残差分解

| # | 断言 | 等级 | 出处 | 是否推翻 Claude 初判 |
|---|---|---|---|---|
| **E1** | **方法与假设已写明**：「去掉子集」松弛为「对绿组细胞赋连续权重」（单纯形约束），最小化目标基因上的加权边际与红组边际的平方差；小基因集另用熵平衡求精确矩解。**松弛严格更宽松，故本节每个残差都是真子集删除情形下残差的下界** | 方法声明 | `RESIDUAL_DECOMPOSITION.md` §1 | 否 |
| **E2** | **求解器已用两个阳性对照标定。** 目标由绿组自身的真实子集生成时，求解器在相同迭代预算下达到机器精度：选择性删除 77%（偏删 `Ccl5` 阳性）残差 **0.0000**、ESS **89.0**；随机删除 60% 残差 **0.0000**、ESS **142.0**。**ESS 精确等于被保留的细胞数** | **[M]** | `results/s3_setA_controls.log` | —— 排除了「维度假象」这一替代解释 |
| **E3** | **因此全基因集的残差 0.701 是结构性的**，不是优化误差也不是维度假象。该值在 2,000／10,000／40,000 次迭代上稳定（目标函数 11.92776 → 11.92775 → 11.92775），末档 L-BFGS 报告收敛成功 | **[M]** | `results/s3_setA_convergence.log` | 否 |
| **E4** | **核心输出：在 6,753 个通过门槛的基因上，去掉绿组的任何子集最多只能对齐绿→红总差异的 29.9%；剩余 ≈ 70% 必须由状态改变承担。** ESS = **152.9**（≥ 30，可信） | **[M]** | `RESIDUAL_DECOMPOSITION.md` §3.2 | 否 |
| **E5** | **窄基因集（8 个趋化因子／驻留标记）可精确匹配，但记 `UNRELIABLE`**：残差 0.000，然而 **ESS = 27.0 < 30**，最大权重／平均权重 = 43.8。按 E2 的标定（ESS ≈ 存活细胞数），**隐含存活 27/385 ≈ 7%，即隐含 f ≈ 0.93** | **[M]** | 同上 §3.1 | 否 |
| **E6** | **§4 与 §2 由两种完全不同的方法得到同一量级。** §2 的 `Ccl5` 要求 f ≥ 0.85；§4 在同批基因上隐含 f ≈ 0.93。**二者一致而非冲突** | 推论 | E5；`CLEARANCE_BOUNDS.md` §2 | 否 |
| **E7** | **残差集中在募集模块。** 闭合份额：`Ccl5` **27.3%**、`Ccl4` **25.9%**、`Ccl3` 55.0%，而 `|差| ≥ 10 pp` 的 640 个基因的中位闭合份额为 **52.0%**（四分位距 34.5–70.1%）。`Ccl5` 与 `Ccl4` 显著低于中位数。另有 17 个基因（2.7%）被联合匹配弄得更差 | **[M]** | 同上 §3.3 | 否 |
| **E8** | **不记 `UNDEFINED`。** 两个基因集上都存在可行解，支持并非不重叠。**「有残差」与「不可行」不可混为一谈** | 条文判定 | 同上 §3.4 | 否 |
| **E9** | **`UNRELIABLE` 的 ESS 门槛（30）是我的选择，非预注册量。** §4 只写「有效 n 过低则不可信」，未给数值。集合 B 的 ESS 为 26.97–29.32，**贴着门槛**；若门槛取 25 则集合 B 转为可信，结论变为「窄基因集上清除可完全解释，但需 f ≈ 0.93」。**请裁定门槛** | 条文缺口 | 同上 §1.3 | —— **规范未写死，执行处补的，须回填** |
| **E10** | **§5 三者并陈**：§2 给区间 0.34–0.85（下限）对 0.13–0.18（点估计）；§3 给可达性——红池绝对数大致恒定，f ≥ 0.6 量级不可达；§4 给残差——清除最多解释 29.9%。**三条独立路径指向同一处**：把募集因子的下降单独归给清除，所需比例既得不到上升侧支持，也与原文的红池动力学不相容 | 推论 | `RESIDUAL_DECOMPOSITION.md` §4 | 否 |
| **E11** | **以上不构成「所以是重编程」的证明。** 本设计不能区分清除与重编程（§7）。已做到的是**给清除解释加上一个它很难满足的定量约束**，不是证否清除、也不是证成其替代 | 限制声明 | 同上 | —— 主动限制结论的射程 |

---

## F. 未做 / 未搜

| 项 | 状态 |
|---|---|
| `GSE221064` 取数 | **`NOT_RETRIEVED`** —— 不在盘上；取数需一次新下载，超出 §1「不检索、不下载」的边界，**待裁定** |
| Fig. S1 C 的 NK 绿／红绝对数 | **`NOT_EXTRACTABLE`** —— 仅存在于图形，未目测取数 |
| 湿实验设计 | **`NOT_DONE`** —— §1 与交接语明令 |
| 主规范 §6 主计算、G-KILL-2 | **`NOT_TOUCHED`** —— 交接语第 4 项 |
| 既有分析与结论文件 | **`UNCHANGED`** —— §1；本任务独立成文 |
| 「下降由清除造成」的归因判定 | **`NOT_MADE`** —— §1 明令；本任务只给约束 |
