# 任务 S-6：IL-15 可及性、受体水平与 STAT5 活性的跨器官测定

**独立 commit。数据为已下载的 Domínguez Conde 跨组织免疫细胞图谱。全部为靶向读取，不重跑全矩阵扫描，不新增下载。**

---

## §0 目的

新的工作假说主张：肿瘤内 NK 募集功能的下调，源于开启募集程序所需的上游输入不足，而非施加了额外抑制。

若该假说成立，则在跨器官层面应当观察到一条对应关系：**IL-15 输入越充足的器官，其 NK 的募集因子表达越高。**

本任务测定这条关系所需的三个量。它们与既有的募集因子检出率来自**同一份数据、同一批供者、同一套流程**，因此器官之间直接可比，无须处理跨数据集混杂。

---

## §1 要算的三组量

### 1.1 输入的供给侧：谁在提供 IL-15

**在各器官的非 NK 细胞中**，计算：

- `IL15` 阳性细胞占该器官非 NK 细胞的比例
- `IL15RA` 阳性细胞占该器官非 NK 细胞的比例
- 上述阳性细胞的**细胞类型构成**（前五位类型及其占比）

**为什么两个基因都要**：IL-15 主要经由 IL-15Rα 反式呈递至 NK，而非游离分泌。`IL15` 反映合成，`IL15RA` 反映呈递能力，二者不可互相替代。**请勿只报其一。**

**范围界定须写明**：「非 NK 细胞」按何种口径界定（作者注释中排除 NK 相关标签，或其他方式），并报告各器官纳入的细胞数。

### 1.2 输入的接收侧：NK 自己接不接得到

**在各器官的 NK 细胞中**，计算 `IL2RB`（CD122）与 `IL2RG` 的检出率。

**这一项的额外价值**：`CD122` 已在第三阶段的流式面板中，此处的转录本结果可作为该面板的预期参照。

### 1.3 输入是否真的到达：STAT5 活性的转录代理

**在各器官的 NK 细胞中**，计算 STAT5 直接靶基因的检出率：`CISH`、`BCL2`、`SOCS1`、`PIM1`。

**这一项比前两项更接近假说本身。** 若假说成立，`CISH` 等应与募集因子在器官间同向变化。**若在动用任何标本之前，跨器官数据本身就显示这种同向关系，则假说在人源数据上已获初步支持。**

**同时报告 `CISH` 与四个募集因子在器官间的秩相关**，并给出自助法区间。

---

## §2 沿用的既有规则

- **可测性带**：<5% 地板、5–85% 可测、>85% 天花板；落带须逐基因逐器官报告。天花板带上行不可测、下行可测但须报起点。
- **统计单元为供者**，非细胞。每供者算一个值，再取供者层面的中位数与四分位距。
- **不得跨化学版本并列排序**。沿用既往通过准入的分层（`10x 5′ v1`、作者注释），其余分层可报但须分开。
- **供者数 < 3 或该器官细胞数 < 200**，记 `INSUFFICIENT`，不给数值。外周血在各化学版本下均仅 2 位供者，预计触发此条。
- 「没找到」记 `NOT_SEARCHED`，永不留空。

---

## §3 输出

**`IL15_AXIS.md`**，一张主表，行为器官，列为：

> 器官 ｜ 供者数 ｜ NK 细胞数 ｜ 非 NK 细胞数 ｜ `IL15`⁺ 比例 ｜ `IL15RA`⁺ 比例 ｜ 提供 IL-15 的主要细胞类型 ｜ NK 的 `IL2RB` ｜ NK 的 `IL2RG` ｜ NK 的 `CISH` ｜ `BCL2` ｜ `SOCS1` ｜ `PIM1` ｜ NK 的 `XCL1`（自既有结果取，供对照）

附：`CISH` 与四个募集因子的器官间秩相关及区间；各量的可测性带标注。

**`CLAIMS_S6.md`** —— 沿用既有格式，含证据等级与「是否推翻 Claude 的初判」一列。

---

## §4 预先写死的判读

| 观察 | 判读 |
|---|---|
| `IL15`／`IL15RA` 供给与 NK 募集因子在器官间同向 | 与假说相容，为其提供人源数据层面的初步支持 |
| `CISH` 等 STAT5 靶基因与募集因子同向 | **比上一条更直接**——说明输入不仅存在差异，且确实到达并被 NK 接收 |
| 供给侧与募集因子无关，但 `CISH` 与募集因子同向 | 输入差异可能不在供给量，而在可及性或受体侧 |
| 各项均无关联 | 假说在跨器官层面得不到支持，如实报告 |

**第四种结果不利于假说，须与其余三种同等对待，不得弱化。**

---

## §5 一处必须随结果一并声明的限制

跨器官的这条关系是**相关，不是因果**。器官之间同时存在大量其他差异，`IL15` 供给与募集功能同向，完全可能由共同的第三因素（如组织的整体免疫活跃程度）造成。

**本任务给出的是「假说在人源数据上是否自洽」，不是「输入决定募集」。** 该限制须写在输出文件的开头。

---

## §6 不做

- 不做因果推断，不做中介分析。
- 不估计任何占比。
- 不重新下载数据，不重跑全矩阵扫描。
- 不碰主规范的 §6 主计算与 G-KILL-2。

---

## 交接语

> 按任务 S-6 执行。
> 1. 本规范作为独立 commit 提交。
> 2. 三组量可一并计算，均为靶向读取；报告读取的列数与耗时。
> 3. 出 `IL15_AXIS.md` 与 `CLAIMS_S6.md`，按 §4 给出判读，§5 的限制置于文件开头。
> 4. **停下等确认。**
