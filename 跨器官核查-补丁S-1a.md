# 补充任务 S-1 · 补丁 S-1a

**接 §2 的循环论证核查结果。独立 commit，不修改 S-1 原文。**

---

## S1a.0 触发

§2 已确认循环论证成立且不轻微：在 `Immune_All_Low` 模型中，`XCL1` 于 CD16⁻（bright）侧权重 +0.1557，在 6,639 个特征中排第 15 位；于 CD16⁺（dim）侧权重 −0.2499，排倒数第 5 位。手工注释标签与该模型的二元一致性为 0.932，因此循环**传导到了实际使用的标签上**。

代理提出的收尾判断成立且重要，此处采纳为本补丁的前提：

> 循环论证同时反对「匹配」与「分解」两条路，因为二者用的都是一个部分编码了结局基因的 bright 标签。

**故在决定匹配还是分解之前，必须先有一个干净的分层变量。** 本补丁只做这一件事。

---

## S1a.1 不接受的一条推断，须撤回

代理以「CCL3 亦为 bright 侧高权重特征（第 3 位），但其与 bright 占比的实测相关为 −0.60，方向相反，故传导并不均匀」来为循环程度定界。

**该推断不成立，须自 `CLAIMS_ORGAN.md` 中撤回或改标为不成立。**

理由：模型权重描述的是「给定某个细胞，该基因高则更可能被判为 bright」；观测相关描述的是「给定某个器官，bright 占比高则该基因检出率高」。二者作用于不同层级，方向不一致属正常，**不能用来推断循环传导的强弱**。

界定循环程度只有一条路径，即 S1a.2：用一个不含目标基因的 bright 定义重算，把这个反推替换为实测。

---

## S1a.2（新增，置于 §3 之前）：构造干净的分层标签

**这是重画标签，不是重跑全矩阵扫描，成本很低。**

### 构造规则

以 `FCGR3A` 与 `NCAM1` 两个基因界定 bright／dim，**明确排除** `XCL1`、`XCL2`、`CCL3`、`CCL4`、`CCL5`，即排除全部本研究的结局基因。

- 阈值须写明，且各器官、各化学版本一致；不得逐器官调阈值。
- 因 `NCAM1` 在 10x 上存在已知 dropout（上一轮统一标记门仅回收 18.6%），须同时报告两套阈值方案的结果：
  1. 严格方案：`NCAM1` 阳性且 `FCGR3A` 阴性 = bright
  2. 宽松方案：仅以 `FCGR3A` 阴性 = bright
  两套并列呈现。**若二者结论相左，本分层变量判为不可靠**，如实记录。
- 落在两者之间、无法归类的细胞单列一格，报告其占比；不得强行归入任一侧。

### 必须报告的一致性

1. 干净标签与 `Manually_curated_celltype` 的**混淆矩阵**（细胞层面）。
2. **各器官的 bright 占比之差**（干净标签 − 原标签），逐器官列出。

### 预先写死的判读

| 观察 | 判读 |
|---|---|
| 各器官 bright 占比之差普遍小于 5 个百分点 | 循环对「占比」这一量的影响有限，上一轮基于占比的数值多数可继续使用，但仍受 §5 区间约束 |
| 差值普遍超过 10 个百分点，或器官间排序发生改变 | 上一轮全部与 bright 占比相关的数值**须整体重估** |
| 介于两者之间 | 记为部分可用，逐器官标注，不作整体判断 |
| 严格与宽松两套方案结论相左 | 该分层变量在转录本层面不可靠，进入 S1a.3 |

---

## S1a.3 若分层在转录本层面不可行

若 S1a.2 判为不可靠，且 §4 显示 `ITGA1`、`ITGAE` 亦落入 5% 以下地板带，则结论为：

> **在转录本层面，本研究所需的 NK 亚群分层无法可靠完成。**

**这是一个干净的结论，不是失败。** 它直接支持把该分层推给流式：流式测的是蛋白，不受 dropout 影响，且面板中 `CD56`、`CD16`、`CD49a`、`CD69` 均已在列。请如实写出，不要尝试用降低阈值或更换基因等方式绕过。

---

## S1a.4 对 §3 的影响

§3 的按器官表**用两套标签各出一份，并列呈现**：一份用 `Manually_curated_celltype`（原标签），一份用 S1a.2 的干净标签。表头须明确标注所用标签。

§4 与 §5 不受本补丁影响，可与 S1a.2 并行执行。

---

## S1a.5 仍然保留的停止条件

**即便 S1a.2 显示干净标签与原标签高度一致，也不要开始匹配或分解。** 分层变量的选择涉及中介与混杂的判断，仍由人决定。S-1 §7 的停止条件继续有效。

---

## 交接语

> 按补丁 S-1a 执行。
> 1. 本文件作为独立 commit 提交，不改动 S-1 原文。
> 2. 先自 `CLAIMS_ORGAN.md` 撤回 S1a.1 所述的那条推断，或改标为不成立。
> 3. 执行 S1a.2，产出干净标签、混淆矩阵与逐器官占比差，按预写判据给出判读；此项完成后先报一次。
> 4. §3 按 S1a.4 出两份表；§4、§5 并行执行。
> 5. **停下等确认。** 不做匹配，不做分解，不重新下载数据。
