# 修正案 A-3 —— 可识别性、标签独立性、以及"选择 vs 重编程"门

**独立 commit。v1.0、A-1、A-2 rev 2 原文均不得改动。**

---

## A3.-1 关于本修正案作者身份的声明（必须先读）

代理拒绝起草 A-3，理由是它已经看过结果，因此它选的任何阈值都是在知道该阈值会纳入或排除什么的情况下选的。**这个理由同样适用于我。** 我已经看过 R2/R1 的 Δ 值、留一法结果、Kish 有效 n、以及 NK-3 的绿细胞数。

因此本修正案的条款按以下方式分类，**每一条都标明**：

| 类型 | 含义 |
|---|---|
| **【结构】** | 判据是结构性的，不含数值阈值，不因看过结果而改变（例：某一侧细胞数为零 → 权重无定义） |
| **【自校准】** | 判据锚定在**该数据集内实测的**噪声底上，而不是我选的数字 |
| **【非盲】** | 我在看过相关结果之后设定的。标出偏倚方向，供后续读者折价 |

**本次唯一的【非盲】条款是 A3.1**，且它的偏倚方向是**不利于**假说的方向——见该条说明。其余全部为【结构】或【自校准】。

---

## A3.0 触发

| 结果 | 触发条款 |
|---|---|
| 亚群标签**由对比本身定义**：NK-3 绿细胞数 **0**，NK-1 绿占 **94.1%**，全体 4,637 细胞共用一个 `X_pca`、一个邻接图、一次 leiden | A3.5 |
| 标准化后 NK-1 权重 0.845 而红侧仅 **8 个细胞**；Kish 有效 n **229 → 11.2（4.9%）**、**421 → 23.3（5.5%）** | A3.4 |
| **NK-3 绿侧为零 → 权重无定义**，不是方差放大 | A3.3 |
| 天花板基因准入**按结果选样**：14 个基因-时间点中 6 个被排除，**六个全是不降的** | A3.1 |
| `Gzmb` NK 72h 移动 **−0.1 pp** 被判"可测" | A3.2 |
| `E-MTAB-6701` 的 `raw_data_10x.txt` 为 **64,735 列**（1 + 64,734 barcode，约 5,000/文库），10x v2 未过滤矩阵应为 **737,280 barcode/文库** → 是 cell-called 矩阵。"raw"指未归一化，**不指未过滤** | A3.6 |
| Dean 自己列出三条出路——分化、外流、原位死亡——**排除了外流，未排除死亡**，且无克隆或谱系追踪，聚类同样是绿红合并做的 | **A3.7** |

---

## A3.1 天花板基因不进任何模块均值 【非盲】

**判据**：>85% 的基因**一律不进入任何模块均值**。其方向与幅度**单独逐个报告**，作为具名观察，并按 A2.2 随附起点与终点检出率、注明为下限估计。模块均值只在 5–85% 带的基因上计算。

**理由**：A2.2 的天花板准入条件在**实现的结果**上——只有下降的基因能进，不降的被排除。14 个天花板基因-时间点中 8 进 6 出，**六个排除项全是不降的**（Prf1 +3.8、Nkg7 +0.2、CD8T Gzmb/Nkg7 +12.1 至 +29.3）。因此任何含天花板基因的模块均值**按构造向下偏**。剔除成员选择的唯一办法是不让它们进均值。

**非盲声明**：本裁定在得知"R1 改为 Ccl3+Ccl4 后 R2/R1 的 Δ 由 +1.3752 增至 +1.7697（24h）、+1.9532 增至 +2.7131（72h），符号不变、幅度更大"**之前**作出，但本文件写于知情之后。**偏倚方向不利于假说**：该条使 Ccl5（最大跌幅项）永久离开 R1 均值，即主动放弃最有利的数字。据此不折价。

**后果**：R1 均值 = CCL3 + CCL4（两基因）。两基因模块的留一法接近空洞但仍执行——翻转说明两基因彼此不一致，照记 `UNSTABLE`。

---

## A3.2 复孔噪声底适用于全部三个带，无例外 【自校准】

A1.3c 的**实测**复孔噪声底适用于**每一个基因、每一个可测带**，包括天花板带的下行与 5–85% 带内的一切变化。低于该数据集自身噪声底的变化，无论落在哪个带，一律记 `UNMEASURED`。

> 触发实例：`Gzmb` NK 72h 由 0.8993 移动到 0.8979（−0.1 pp）被判可测——A2.2 给了天花板带下行以合法性，却没给它幅度门槛。本条补上，且不引入新数字。

---

## A3.3 支持不重叠 → `UNDEFINED`（独立于 `UNIDENTIFIABLE`）【结构】

**新增判定类别。** 若某一亚群在被比较的两侧中，任一侧的细胞数为 **0**，则跨该组成的标准化**无定义**——不是噪声大，是权重为无穷。

`UNDEFINED` 与 `UNIDENTIFIABLE` 是两种不同的失败：

| 类别 | 触发 | 能否用阈值救 |
|---|---|---|
| `UNIDENTIFIABLE` | 重加权后有效 n 过低（A3.4） | 理论上可以，加数据 |
| **`UNDEFINED`** | **某亚群在一侧为零** | **不能。任何有效 n 阈值都抓不到它** |

**处置**：出现 `UNDEFINED` 时，禁止报告任何组成校正后的量，禁止用"去掉该亚群"绕过（那是在结果已知的情况下改变估计对象）。**必须报告该亚群的存在及其零侧**，并说明分解为何不做。

> 触发实例：NK-3 绿侧 0 细胞，红侧 57。

---

## A3.4 可识别性判据 —— 锚定噪声底，不用固定阈值 【自校准】

**判据**：组成标准化后的估计量，其标准误必须**小于该数据集内实测的复孔噪声底**（A1.3c）。不满足 → 记 `UNIDENTIFIABLE`。

**为何这样写而不写一个百分比**：我已经看过 4.9% / 5.5% 这两个数，任何我挑的保留率阈值都带着这个知识。锚定在实测噪声底上的判据不需要我挑数字，且随数据集自动缩放——数据越干净，能容忍的重加权越激进。

**强制并列报告**（无论是否通过）：每个权重 > 0.20 的亚群，在**两侧各自**剩余的细胞数；以及两侧的 Kish 有效 n 与保留率。

---

## A3.5 亚群标签独立性 —— **构造要求**，不是检查项 【结构】

**这一条不是在分解之前查一下，而是在建标签的时候就必须满足。** 作为检查项它在手上的两个数据集上都不通过，然后把人卡死；作为构造要求它可以被满足。

**允许的两种构造，二选一，必须预先声明**：

1. **单侧聚类 + 投影** —— 只在一侧（如绿侧）做降维、建图、聚类，再把另一侧投影到该空间并分配标签。
2. **规范标记定义** —— 用与本对比无关的、文献既定的标记面板定义亚群（如 CD49a / CD49b / CD27 / CD11b / KLRG1），不做无监督聚类。

**禁止**：在合并的两侧细胞上做单一 PCA / 单一邻接图 / 单一 leiden，然后把所得标签用于组成-状态分解。那样标签本身部分由对比定义，分解是循环论证。

> 触发实例：`E-MTAB-10176` 全体 4,637 细胞共用一个 `X_pca`、一个邻接图、一次 leiden；NK-1 绿占 94.1%、NK-3 绿占 0.0%。三个亚群里有两个 ≥90% 由颜色决定。

**追溯**：任何使用作者提供的、由合并聚类得到的亚群标签所做的分解，一律不进入 `RESULTS.md`。

---

## A3.6 §4(a) 的实证判据 —— 数 barcode，不读文件名 【结构】

**新增 §4(a′)**：判定一个矩阵是否保留空液滴，**必须**用 barcode 计数对照平台理论上限，不得依据文件名、字段名或作者措辞。

| 平台 | 每文库未过滤 barcode 数（量级） |
|---|---|
| 10x v2 | 737,280 |
| 10x v3 | ~6.8 M |

barcode 数落在每文库数千量级 = **cell-called 矩阵**，无论它叫 `raw`、`raw_data`、还是 `adata.raw`。

> **同一个陷阱第二次触发。** 第一次是 `GSE302113`（模态级检查全过、ascertainment 被剥离）；这次是 `E-MTAB-6701` 的 "raw" 指未归一化而非未过滤。**统计量与模态的区分要用数字确认，不能用命名确认。**

**追溯效果**：`E-MTAB-6701` 的暂定通过**撤销**——它通过 (f) 与 (g)，但**不通过 (a)**，因此 soup fraction 不可估，R1 与 R2 的环境 RNA 负担无法并列报告。见 A3.8。

---

## A3.7 新增门 G-KILL-4 —— 选择 vs 重编程（本修正案最重要的一条）

### 命题

"肿瘤 NK 的 R1 输出下降"有两个互斥解释：

| | 机制 |
|---|---|
| **重编程** | 同一批细胞改变了表达 |
| **选择** | 表达 R1 的亚群被优先清除，剩下的从未高表达 R1 |

**两者在横断面数据里不可分。** Kaede 提供的是**时间戳**，不是**谱系**；没有克隆追踪或死亡读数，绿→红是群体层面的相关，不是逐细胞的轨迹。

### 现状

Dean **公开报告了**组成梯度（Fig 1D 即"各簇内 Kaede Green⁺ 与 Kaede Red⁺ 的比例"，NK_1 几乎全为 KG⁺、NK_5 约 90% KR⁺），并**正面列出了三条出路**：分化为 CD49a⁺ 状态、外流出瘤、或原位死亡。他**用其他组织中的 KR⁺ NK 排除了外流**。

**他没有排除死亡。** 论文中无克隆或谱系追踪。

> 这个检查的结果比预期**对 Dean 更公平**：他不是忽略了这个可能，他是列出来、关掉了一扇门、留下了另一扇。**结论不是"他的框架错了"，而是"有一扇具体的门没关"。** 这是更好的东西——窄，且可检验。

### 结构性障碍：scRNA-seq 无法回答

凋亡细胞在常规 QC（高线粒体比例、低基因数、低 UMI）中被剔除。**能证明选择性死亡的细胞按构造已经被移除。** 因此**任何公共 scRNA-seq 数据都无法关上这扇门——包括 Dean 自己的**。不要试图用生信关它。

### 判定规则（强制）

在选择被排除之前：

- **禁止**把 R1 下降或 R2/R1 上升表述为程序改变、重编程、沉默、或受众缩窄的**机制**。
- **允许**的表述只有观察层面的：**"肿瘤中表达 R1 的 NK 比例下降"**。

### A2.6 命题因此一分为二

| 层 | 陈述 | 状态 |
|---|---|---|
| **观察层** | 肿瘤中不再有 NK 发出 CCR1/CCR5 轴的呼叫，而 XCR1 轴的呼叫保留 | **对两种机制都成立**，不受本门约束 |
| **机制层** | NK **改变了程序**，缩窄了可寻址的受众 | **受本门约束**，未关门前不得陈述 |

若最终是选择，A2.6 的机制层必须改写为"**发出广谱招募信号的 NK 在肿瘤中被优先清除**"——那是一个不同的命题，落在存活轴而非调节轴，需要不同的实验。**但观察层的功能后果相同**，所以方向不因此死亡，只是换了机制归属。

### 能关门的实验（都不是生信）

- Kaede 绿 vs 红 NK 的 cleaved caspase-3 / Annexin V（原位或流式）
- 死亡阻断臂（NK 特异 Bcl-2 过表达，或 caspase 抑制剂）后组成位移是否消失
- 克隆条码 / 谱系追踪

### 两面性（必须同时持有）

1. **风险**：若是选择，A2.6 的机制层归零。
2. **机会**：**这是持有该系统的那个组用他们自己的工具关不上的门。** 它需要一个他们没做的实验，而不是一次更好的再分析。这是本方向目前唯一有实验价值的入口。

---

## A3.8 数据集清单状态

| 数据集 | (a) 未过滤 | (f) R1/R2 可测 | (g) 复孔 | 判定 |
|---|---|---|---|---|
| `E-MTAB-10176` | — | R2 ✓ / K ✗ | ✗（n=1 每侧） | **`INADMISSIBLE`**，仅作记录 |
| `E-MTAB-6701`（VT2018） | **✗（A3.6 撤销）** | ✓（CCL3 0.817、CCL5 0.585、XCL1 0.756） | ✓（7 供者） | **`INADMISSIBLE` on (a)** |
| `GSE221064` | ✗（仅 filtered） | 未定 | **部分 ✓**（2 绿生物学重复 + 1 红；红侧仍 n=1） | 不通过 (a) |
| `GSE154826` | **✓**（真未过滤 droplet） | **未定** | 29 对配对 | **唯一可能三项全过** |

**当前无任何候选同时通过 (a)(f)(g)。**

**本轮唯一授权的计算**：在 `GSE154826` 上对 NK 计算 **CCL3 / CCL4 / CCL5 / XCL1 的检出率**，判定 (f)。**只做准入判定，不算 R1、不算 R2、不算比值、不做任何组间比较。**

**若 (f) 通过，须同时记录的三件事**（不是本轮任务，是提前写死）：
1. `GSE154826` **无 Kaede**，所以它能做的对比是**同一患者的肿瘤 NK vs 邻近肺 NK**，不是绿/红。这是**另一个对比**，回答的是"同一患者体内肿瘤 NK 的 R1 是否低于邻近肺 NK"，不是"进入肿瘤的 NK 是否失去 R1"。两者不得混用措辞。
2. 29 对配对供者是本项目迄今唯一像样的 n——**n 按供者算，不按文库算**（§4(d)）。
3. **A3.7 的门同样适用于该对比**：肿瘤与邻近肺的 NK 组成差异同样可由选择产生。

---

## A3.9 未改变的部分

**v1.0**：§0 跨区室绝对比较禁令、§2 门结构、§7 检出率单位与恒等式闭合、§8 输出格式、§9 自检清单 —— 不变。
**A-1**：A1.1 撤销、A1.3 全部条款、A1.6 —— 不变。
**A-2 rev 2**：模块定义（R1 = CCL3/CCL4/CCL5，R2 = XCL1 单基因，K 不变）、主读数 R2/R1、规则 2′、A2.2 的方向性天花板、A2.3 的 (f)(g)、A2.4、A2.6 命题措辞（现按 A3.7 分为观察层与机制层）、A2.7 预测 —— 不变。

**关于后续修改的说明**：v1.0 之后已有 A-1、A-2 rev 2、A-3 三份修正案，判据分散在四个文件里。**下一次需要改动判据时，做一次合并的 v2.0，不要出 A-4。**

---

## 交接语

> 按修正案 **A-3** 执行。
> 1. **A-3 作为独立 commit 提交**，不改动 v1.0、A-1、A-2 rev 2。
> 2. 按 A3.1 重报 `E-MTAB-10176` 的 R1（= Ccl3 + Ccl4），天花板基因单列。仍写 `EXCLUDED.md`。
> 3. 按 A3.3 / A3.4 / A3.5 把 `E-MTAB-10176` 的分解判定为 `UNDEFINED`（NK-3）与 `UNIDENTIFIABLE`（有效 n），并记录 A3.5 的构造违规。
> 4. **唯一授权的计算**：`GSE154826` 上 NK 的 CCL3 / CCL4 / CCL5 / XCL1 检出率，判定 (f)。**只判准入，不算任何比值或组间差异。**
> 5. **停下等我确认。** 不跑 G-KILL-2、G-KILL-3 剩余两问、G-KILL-4，不进 §6 主计算。
