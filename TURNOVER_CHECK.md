# TURNOVER_CHECK.md —— S-3 §3：实测周转率与所需清除比例的比对

> ## 📌 S-4 §0 的判定如何影响本文件
> S-3 的「清除 vs 状态改变各占几成」框架已被 S-4 §0 判为**不成立**（`CLAIMS_S4.md` A1）。
> **本文件不受影响**：它给出的是红池动力学的**边界**（原文 **[V]**：Kaede Red⁺ 绝对数在 5 天内大致恒定，而红池只减不增），
> 这条边界约束的是「若为清除，需要多大比例」，**不依赖把观察拆成两个可加成分**。按 `CLAIMS_S4.md` A3，本文件的结论**仍然成立**。
> 唯一须改的读法：它所约束的那些 f 值出自 `CLEARANCE_BOUNDS.md`，那些 f **不得再当作占比使用**。



> ## ⚠ 贯穿全文的限制（§7，置于开头）
> **Kaede 的绿／红对比无法区分「清除」与「重编程」。** 本文件比对的是「§2 需要清掉多少」与「原文能支持多少」，**不是**在证明或否证清除机制本身。

---

## 0. 数据可得性

| 数据集 | 状态 |
|---|---|
| **`GSE221064`**（Dean，MC38 Kaede，48 h） | **不在盘上，本轮未取数。** `data/raw/` 无任何相关文件；`DATASETS.md` 记为 `NOT_RUN`（仅 filtered matrix，判据 (a) 不通过，原始 FASTQ 在 `PRJNA912695`）。§3 中凡涉及该数据集的项一律记 `NOT_RETRIEVED`，**未以其它数据代填** |
| **`E-MTAB-10176`**（Kaede，24／72 h） | 在盘上，`DW_T_NK.h5ad`。本文件全部数字出自此处 |
| **原文** | `E-MTAB-10176` 的 `idf` 记 PubMed **35472220** = *In vivo labeling reveals continuous trafficking of TCF-1⁺ T cells between tumor and lymphoid tissue*，**J Exp Med 2022**，PMC9048291（开放获取，全文 XML 已读）|

---

## 1. 绿／红细胞数与各文库计数

| 文库 | 颜色 | 时点 | 文库总细胞 | 其中 NK |
|---|---|---|---|---|
| `MH1_G24` | 绿 | 24 h | 354 | **97** |
| `MH2_R24` | 红 | 24 h | 1,433 | **229** |
| `MH3_G72` | 绿 | 72 h | 781 | **288** |
| `MH4_R72` | 红 | 72 h | 2,069 | **421** |

绿／红 NK 数之比：24 h **97 : 229 = 0.42**；72 h **288 : 421 = 0.68**。

### 1.1 这两个比值**不能**用作周转率——原因是设计层面的

`E-MTAB-10176` 的 `sdrf` 显示：四个样本各自是一个独立的 10x 文库，分选门为 `CD45+ Ter119− CD11b−`，**绿与红被分选进彼此分开的文库**（`Green24` / `Red24` / `Green72` / `Red72`，各带 4 条测序泳道，泳道是技术重复而非生物学重复）。

> **绿∶红的细胞数之比因此只反映每个文库上机了多少细胞，与瘤内真实丰度无关。**
> 该比值在本数据集里**不是**一个可解释的生物学量，本文件不据此计算任何周转率。

这同时再次确认了 `CLEARANCE_BOUNDS.md` §0.2 的两点：颜色与文库完全共线，且**全库无同色复孔**。

### 1.2 一处方向性的旁证（不作定量使用）

原文正文（**[V]**）：「At 24 h after photoconversion **∼25%** of CD8 T cells, CD4 T cells, Treg, and **natural killer (NK) cells were Kaede Green⁺**」。本数据 24 h 的 NK 绿占比为 97/326 = **29.8%**，与之同量级。但按 §1.1，这是分选比例的产物，**同量级只是巧合层面的一致，不构成验证**。

---

## 2. 原文是否报告过 NK 在瘤内的存活或周转

### 2.1 报告了什么（**[V]**，全文 XML 实读）

| 项 | 原文出处 | 内容 |
|---|---|---|
| NK 的 Kaede 绿占比随时间 | Fig. 1 C、Fig. S1 B | 24 / 72 / 120 h 三个时点，**流式**，NK 门为 `CD11b^−/lo CD11c⁻ CD3⁻ CD4⁻ CD8⁻ NK1.1⁺` |
| NK 的 Kaede 绿⁺ 与红⁺ **绝对细胞数** | **Fig. S1 C** | 24 / 72 / 120 h 三个时点分细胞类型报告 |
| NK 的样本量 | Fig. 1 D、Fig. S1 B–C | **n = 9（24 h）、5（72 h）、3（120 h）** 只小鼠，合并自六次独立实验 |
| 整体趋势的文字结论 | 正文 | 「The total number of Kaede Green⁺ TILs **increased 10-fold** over the 5-d timeframe… In contrast, **the total number of Kaede Red⁺ cells appeared relatively constant.** Thus the steady drop in the proportion of Kaede Red⁺ cells was explained by the **continuous recruitment** of new cells into the tumor」 |
| 增殖对 Kaede Red 蛋白稀释的影响 | Fig. S1 标题 | 「**Limited intratumoral T cell proliferation** prevents rapid loss of Kaede Red protein」 |

### 2.2 **没有**报告什么（如实记录，未以推测填补）

| 项 | 状态 |
|---|---|
| 凋亡／活力测定（Annexin V、caspase、活死染色） | **`NOT_REPORTED`** —— 全文 `apoptos` 0 次、`viabilit` 0 次、`Annexin` 0 次、`caspase` 0 次、`death`／`dying` 0 次 |
| 「turnover」一词 | **`NOT_REPORTED`** —— 全文 0 次 |
| NK 在瘤内的**死亡率** | **`NOT_REPORTED`** —— 无任何直接测定 |
| **NK 特异**的绿⁺／红⁺ 绝对数值 | **`NOT_EXTRACTABLE`** —— 数值只存在于 Fig. S1 C 的图形中，正文与图注均未给数字。本文件不从图形目测取数 |
| `GSE221064` 的对应各项 | **`NOT_RETRIEVED`** —— 数据集未取数（§0） |

**因此本节只能给边界，不能给点值**，与 §3 的要求一致。

---

## 3. 边界：原文能支持多大的清除比例

### 3.1 关键的结构性事实

Kaede 光转换在 t = 0 把当时在瘤内的细胞全部标红。此后：

- **新进入的细胞是绿的，不会变红。**
- **红池因此是一个封闭的、不可补充的队列**——它只能因死亡或迁出而变小，不能因募集而变大（增殖只会使其变大并稀释蛋白，原文已单独处理，见 Fig. S1 标题）。

### 3.2 由此得到的边界

原文（**[V]**）：**「the total number of Kaede Red⁺ cells appeared relatively constant」**，跨 24 → 120 h（即 96 小时）。

> 一个**只减不增**的封闭队列，其绝对数在 96 小时内「大致恒定」，意味着**该窗口内的净损失很小**。
> §2 要求的清除比例是 **f = 0.34（`Ccl3`）／0.60（`Ccl4`）／0.85（`Ccl5`）**，且这是**下限**。
> **在 48 小时窗口内清掉三成到八成半的红池，与「红池绝对数大致恒定」不相容。**

### 3.3 这条边界的四点限制，必须一并带走

1. **「大致恒定」是定性表述**，原文未给数值；NK 特异的曲线在 Fig. S1 C 中，本文件未从图形取数（§2.2）。
2. **该句的主语是 TILs 整体**，虽然 Fig. S1 C 按细胞类型分列，但**正文的定性结论未按 NK 单独复述**。故本边界对 NK 的适用性是**由整体外推的**，等级低于直接测定。
3. **净损失 ≠ 死亡。** 红池的减少同时包含**迁出**与**死亡**；原文的主旨恰恰是「持续的双向 trafficking」，即迁出确实存在。故「红池损失小」给出的是 **死亡 + 迁出之和** 的上界，死亡单独只会更小——**这个方向对本节的结论有利，不需要额外让步**。
4. **NK 的样本量极小**（n = 9 / 5 / 3 只）。任何基于该曲线的定量主张都受此限制；本文件只用其定性方向。

---

## 4. §3 的预写落点

> §3 原文：「若 §2 得出的 f **显著高于**任何可支持的实际清除水平，则**纯清除解释不成立**，这本身是一个有价值的结论，请如实写出。」

**该条触发。** 如实写出：

> ### 纯清除解释与原文报告的红池动力学不相容。
> - §2 的下限要求：`Ccl5` **f ≥ 0.85**、`Ccl4` **f ≥ 0.60**、`Ccl3` **f ≥ 0.34**（且这些是最有利于清除解释的极端假设下的下限）。
> - §2 上升侧给出的自洽比例只有 **≈ 0.13–0.18**。
> - 原文对红池的报告是「绝对数大致恒定」，而红池是只减不增的封闭队列。
>
> 三者之中，**没有任何一个支持 f ≥ 0.6 这一量级**。

**但这条结论的证据等级须写准**：它建立在一句**定性**的、**关于 TILs 整体**的原文表述之上（§3.3 的限制 1、2），并非由 NK 特异的定量周转率导出。**它足以否定「纯清除」这一强主张，不足以给出「实际清除了百分之几」的点值。**

---

## 5. 未做

- 未从图形目测取数（Fig. S1 C 记 `NOT_EXTRACTABLE`）
- 未下载 `GSE221064`；其各项记 `NOT_RETRIEVED`，未以 `E-MTAB-10176` 代填
- 未把绿∶红文库细胞数之比当作周转率使用（§1.1）
- 未把「净损失」等同于「死亡率」（§3.3 限制 3）
- 未做湿实验设计
