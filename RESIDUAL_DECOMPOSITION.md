# RESIDUAL_DECOMPOSITION.md —— S-3 §4：加权匹配的残差分解

> ## ⚠ 贯穿全文的限制（§7，置于开头）
> **Kaede 的绿／红对比无法区分「清除」与「重编程」。** 本文件问的是「**去掉绿组的任何一个子集，最多能对齐多少**」，答案的补集即「清除解释不了、必须由状态改变承担的份额」。这**不是**在证明发生了清除。

---

## 1. 方法与假设（§4 要求写明）

### 1.1 问题的形式化

设绿组 NK 细胞为 $\{x_i\}_{i=1}^{385}$（$x_i \in \{0,1\}^J$ 为检出向量），红组的边际检出率为 $t \in [0,1]^J$。求权重 $w$ 落在单纯形上（$w_i \ge 0$，$\sum w_i = 1$），使

$$\min_w \; \sum_j \Big( (X^\top w)_j - t_j \Big)^2 .$$

**「去掉一个子集」被松弛为「对绿组细胞赋连续权重」。** 松弛严格地更宽松（0/1 权重是连续权重的特例），因此**本文件给出的每一个残差都是「真按子集删除」情形下残差的下界**——真实残差只会更大。

### 1.2 两个求解器

| 求解器 | 用途 | 说明 |
|---|---|---|
| **熵平衡**（Hainmueller） | 小基因集 | 在**精确矩约束**下取最大熵权重，对偶问题 $\min_\lambda \log\sum_i e^{-\lambda^\top x_i} + \lambda^\top t$。收敛即说明该基因集上存在一个能精确复现红组的清除模式 |
| **最佳匹配** | 两个基因集 | $w = \mathrm{softmax}(\theta)$，L-BFGS 最小化上式。给出**任何**重加权所能闭合的最大份额 |

参数化为 softmax 自动满足非负与归一，无需投影。

### 1.3 有效样本量与不可信判据

Kish 有效样本量 $\mathrm{ESS} = (\sum w)^2 / \sum w^2$。

**判据：ESS < 30 → 记 `UNRELIABLE`。**

> **【S-3a 更新】** 该门槛已由补丁 S-3a §3 回填并**维持为 30**，附非盲声明（偏倚方向**不利于假说**，须折价）与四档对照，见本文件 S-3a-3 节。

### 1.4 目标基因集（§4 要求两种都做）

| 集合 | 内容 | n |
|---|---|---|
| **A** | §2 门槛通过的**全部**基因 | **6,753** |
| **B** | 趋化因子 + 驻留标记子集：`Ccl3`、`Ccl4`、`Ccl5`、`Xcl1`、`Itga1`、`Cd69`、`Rgs1`、`Cd160` | **8** |

集合 B 中被点名但**未通过 §2 门槛、故排除**：`Xcl2`、`Itgae`、`Cxcr6`、`Zfp683`（如实记录，未降门槛纳入）。

---

## 2. 求解器的标定：两个阳性对照

**必须先回答一个问题**：残差 0.70 究竟是「清除模型真的解释不了」，还是「用 385 个点匹配 6,753 个矩本来就做不到」？故构造两个阳性对照——目标由绿组自身的一个**真实子集**生成，此时按构造存在一个 0/1 权重可精确匹配。

| 对照 | 构造 | 真实 f | L1 残差 | 闭合份额 | ESS | 目标函数 |
|---|---|---|---|---|---|---|
| **对照 1（选择性）** | 以 0.85 / 0.15 的概率优先删去 `Ccl5` 阳性细胞，保留 89/385 | **0.769** | **0.0000** | **100.0%** | **89.0** | 3.2 × 10⁻¹⁶ |
| **对照 2（随机）** | 随机删去 60%，保留 142/385 | 0.631 | **0.0000** | **100.0%** | **142.0** | 1.5 × 10⁻¹⁶ |
| 真实红组目标 | —— | —— | **0.7010** | **29.9%** | 152.9 | 1.19 × 10¹ |

三行**在相同的 2,000 次迭代预算下**求解。两个对照均达到机器精度，且 **ESS 精确等于被保留的细胞数**（89.0、142.0）——这同时验证了两件事：

1. **求解器能精确还原真实子集**，包括一个删掉 77% 细胞的**强选择性**子集。故 6,753 个矩、385 个细胞这一维度组合**本身不构成障碍**。
2. **ESS 可以读作「隐含的存活细胞数」**，因为在真子集情形下二者恰好相等。

> **因此真实红组的 0.7010 是结构性的，不是优化误差、也不是维度假象。**

补充：真实目标下 L1 残差在 **2,000 / 10,000 / 40,000** 次迭代上稳定为 **0.7010**（目标函数 11.92776 → 11.92775 → 11.92775），第三档 L-BFGS 报告收敛成功。

---

## 3. 结果

| 基因集 | 求解器 | 基因数 | **ESS** | 可信？ | **L1 残差** | **可闭合份额** |
|---|---|---|---|---|---|---|
| B | 熵平衡（精确矩） | 8 | 29.3 | **否**（< 30） | 0.251 | 74.9% |
| **B** | **最佳匹配** | 8 | **27.0** | **`UNRELIABLE`** | **0.000** | **100.0%** |
| **A** | **最佳匹配** | **6,753** | **152.9** | **是** | **0.701** | **29.9%** |

### 3.1 集合 B：能精确匹配，但代价是 `UNRELIABLE`

在 8 个趋化因子／驻留标记上，重加权**可以精确复现红组**（残差 0.000）。但它靠的是把权重压到极少数细胞上：

- **ESS = 27.0**，低于本文件声明的 30 → 记 **`UNRELIABLE`**
- 最大权重 / 平均权重 = **43.8**
- 按 §2 的标定（ESS ≈ 存活细胞数），**隐含的存活比例为 27/385 ≈ 7%，即隐含 f ≈ 0.93**

> **这与 §2 的独立结论一致而非冲突**：§2 的 `Ccl5` 需要 f ≥ 0.85；§4 在同一批基因上用完全不同的方法得到隐含 f ≈ 0.93。**两条路都指向一个「必须清掉九成」的量级。**

### 3.2 集合 A：可信，且残差很大

| 项 | 值 |
|---|---|
| ESS | **152.9**（≥ 30 → 可信） |
| 最大权重 / 平均权重 | 7.3 |
| **L1 残差** | **0.701** |
| **可闭合份额** | **29.9%** |
| L2 残差 | 0.677 |

> ### 核心输出
> **在全部 6,753 个通过门槛的基因上，去掉绿组的任何子集最多只能对齐绿→红总差异的 30%。**
> **剩下的 ≈ 70% 是清除解释不了的份额，必须由状态改变承担。**
> 且这是**下界**：连续权重比真子集删除更宽松（§1.1），真按子集删只会更差。

### 3.3 逐基因：残差集中在哪里

| 基因 | 绿 | 红 | 匹配后 | 原始差 (pp) | 残差 (pp) | 闭合 |
|---|---|---|---|---|---|---|
| **`Ccl5`** | 0.904 | 0.346 | 0.752 | **+55.8** | **+40.5** | **27.3%** |
| **`Ccl4`** | 0.701 | 0.262 | 0.588 | **+44.0** | **+32.6** | **25.9%** |
| **`Ccl3`** | 0.403 | 0.098 | 0.235 | **+30.4** | **+13.7** | **55.0%** |
| `Xcl1` | 0.494 | 0.574 | 0.530 | −8.0 | −4.4 | 45.8% |
| `Itga1` | 0.083 | 0.292 | 0.120 | −20.9 | −17.2 | 17.9% |
| `Rgs1` | 0.730 | 0.838 | 0.690 | −10.9 | −14.8 | **−36.6%**（更差） |
| `Cd160` | 0.099 | 0.162 | 0.093 | −6.3 | −6.8 | −8.8%（更差） |
| `Cd69` | 0.621 | 0.609 | 0.532 | +1.2 | −7.7 | 更差 |

`|原始差| ≥ 10 pp` 的 **640** 个基因：闭合份额中位数 **52.0%**，四分位距 [34.5%, 70.1%]；其中 **17 个（2.7%）被匹配弄得更差**——联合匹配是一个折中，个别基因会被牺牲。

> **两个募集因子 `Ccl5`（27.3%）与 `Ccl4`（25.9%）的闭合份额低于全体中位数 52.0%。** 与 §2 一致：**清除解释力最弱的地方，恰是募集模块本身。**

### 3.4 `UNDEFINED` 的判定

§4 规定：若绿组中不存在任何子集能匹配红组（支持不重叠），记 `UNDEFINED`。

**本轮不记 `UNDEFINED`。** 两个基因集上都存在可行解（B 精确、A 部分），支持并非不重叠。A 的 0.701 是**残差**，不是不可行——**这两者不可混为一谈**：A 的解存在且已找到，只是它离红组还差 70%。

---

## 4. 与 §2、§3 的合并呈现（§5 要求三者并陈）

| 侧面 | 结果 |
|---|---|
| **§2 约束区间** | 下降侧下限：`Ccl3` **0.34** / `Ccl4` **0.60** / `Ccl5` **0.85**。上升侧点估计：中位数 **0.13–0.18**，10–90% [0.058, 0.367]〔**S-3a 撤销 T差 筛选后：中位数 0.182，10–90% [0.053, 0.370]**——见 `CLEARANCE_BOUNDS.md` S-3a-1.3，结论未变〕 |
| **§3 生物学可达性** | 原文（JEM 2022，**[V]**）：Kaede Red⁺ 细胞**绝对数在 5 天内大致恒定**；红池是只减不增的封闭队列 → **f ≥ 0.6 这一量级不可达**。限制：定性表述、主语为 TILs 整体、净损失含迁出、NK 样本量 n = 9/5/3 |
| **§4 残差** | 全基因集：**清除最多解释 29.9%，≈ 70% 须由状态改变承担**（ESS 152.9，可信，且为下界）。窄基因集：可精确匹配但隐含 f ≈ 0.93，ESS 27.0 → `UNRELIABLE` |

**三者相互独立，指向同一处**：把募集因子的下降**单独**归给清除，所需的清除比例在 0.34–0.93 之间，而该量级既得不到上升侧的支持（0.13–0.18），也与原文报告的红池动力学不相容。

**但仍须回到 §7**：以上**不构成**「所以是重编程」的证明。本设计不能区分二者；它能做的、也已经做到的，是**给清除解释加上一个它很难满足的定量约束**。

---

## 5. 未做（S-3）

- 未断言下降由清除造成，也未断言由重编程造成（§1、§7）
- 未把连续权重的残差当作真子集删除的残差使用（§1.1 已声明其为下界）
- 未在 ESS < 30 的结果上给出定量结论（集合 B 记 `UNRELIABLE`）
- 未把「有残差」误记为 `UNDEFINED`（§3.4）
- 未降低 §2 门槛以纳入 `Xcl2`／`Itgae`／`Cxcr6`／`Zfp683`
- 未下载 `GSE221064`

**S-3a 未新增计算**——S-3a-3 与 S-3a-4 全部由既有结果推出，未重跑求解器、未重新下载数据。

产出：`results/s3_residual_summary.csv`、`results/s3_residual_per_gene.csv`、`results/s3_residual_per_gene_all.csv`、`results/s3_residual.log`、`results/s3_setA_convergence.log`、`results/s3_setA_controls.log`；脚本 `scripts/s3_residual_decomposition.py`。


---
---

# 补丁 S-3a 的更新（本节以下为 S-3a 内容，S-3 原文未改动）

## S-3a-3. 回填门槛二：加权匹配的有效样本量下限

### S-3a-3.1 回填规则与非盲声明

> **判据：ESS ≥ 30 记为可用；ESS < 30 记 `UNRELIABLE`。维持原门槛，不作调整。**（S-3a §3 回填）

**非盲声明**：本门槛由执行处在**已知 ESS 实测值之后**设定（S-3 §1.3 已如实报告），协议方在**已知其贴近门槛**的情况下予以维持，属非盲。偏倚方向：

- 维持 30 意味着集合 B 保持 `UNRELIABLE`，其隐含 **f ≈ 0.93 不进入结论**；
- 该 f 值若进入结论，将**支持**清除解释，即支持原本的工作假说；
- 故本门槛的**偏倚方向不利于假说**。

> **须据此折价。** 下列三项为因偏倚方向不利而补充的报告（S-3a §3）。

### S-3a-3.2 补充一：集合 B 的结果照常完整报告

**不因不可用而略去。**

| 求解器 | 基因数 | 残差 L1 | 可闭合份额 | **ESS 实测** | 隐含存活 | **隐含 f** | 判定 |
|---|---|---|---|---|---|---|---|
| 最佳匹配 | 8 | **0.000** | **100.0%** | **26.98** | 27.0 / 385 = 7.0% | **0.930** | **`UNRELIABLE`**（ESS < 30） |
| 熵平衡（精确矩） | 8 | 0.251 | 74.9% | **29.32** | 29.3 / 385 = 7.6% | **0.924** | **`UNRELIABLE`**（ESS < 30） |

最大权重／平均权重 = 43.8（最佳匹配）。隐含 f 按 §2 的标定读出（阳性对照中 **ESS 精确等于被保留的细胞数**，89.0 与 142.0）。

### S-3a-3.3 补充二：四档 ESS 门槛下集合 B 的可用性与隐含 f

| ESS 门槛 | 最佳匹配（ESS = 26.98） | 熵平衡（ESS = 29.32） | 若可用，进入结论的隐含 f |
|---|---|---|---|
| 20 | **可用** | **可用** | **0.930 / 0.924** |
| 25 | **可用** | **可用** | **0.930 / 0.924** |
| **30（回填维持）** | **`UNRELIABLE`** | **`UNRELIABLE`** | —— 不进入结论 |
| 35 | `UNRELIABLE` | `UNRELIABLE` | —— 不进入结论 |

**门槛在 25 与 30 之间翻转。** 若取 25 或更低，集合 B 转为可用，结论增加一句：「在 8 个趋化因子／驻留标记上，清除可以完全解释绿→红差异，**但需要 f ≈ 0.93**」。

**须注意该句仍然不支持纯清除解释**：f ≈ 0.93 意味着 48 小时内清掉九成三的瘤内 NK，而 `TURNOVER_CHECK.md` §3 的边界（原文报告 Kaede Red⁺ 绝对数在 5 天内大致恒定，红池只减不增）**正是与这一量级不相容的那一条**。**即：放松 ESS 门槛会让集合 B 进入结论，但进入之后它给出的 f 反而被 §3 挡住。**

### S-3a-3.4 补充三：集合 A 与集合 B 问的不是同一个问题（**比 ESS 门槛更根本**）

| | 集合 B | 集合 A |
|---|---|---|
| 约束数（基因） | **8** | **6,753** |
| 自由变量（绿组细胞） | 385 | 385 |
| 自由度余量 | **377** | **−6,368**（严重超定） |
| 能否精确匹配 | **能**——但这是**构造使然** | 不能，残差 0.701 |

> **集合 B 的「精确匹配」不是一个发现，是一个算术事实。**
> 用 385 个自由变量去匹配 8 个边际，自由度余量高达 377；只要目标落在绿组细胞的凸包内，几乎必然存在解。**它证明的是「存在某组权重能对上这 8 个数」，不是「构成层面能解释全部差异」。**
>
> 集合 A 才是构成层面的检验：同样 385 个自由变量，要同时对上 6,753 个边际。**在那里，去掉绿组的任何子集最多只能对齐 29.9%。**
>
> **这一点独立于 ESS 门槛。** 即便把门槛降到 20、让集合 B 完全可用，集合 B 的 100% 闭合率也**不能**用来反驳集合 A 的 29.9%——两者不是同一个量，前者是 8 个数上的拟合，后者是全转录组构成上的检验。**把前者当作后者的反例，是自由度层面的误读。**

**并且，§2 的阳性对照已经排除了「集合 A 只是自由度不够」这一替代解释**：目标由绿组的**真实子集**生成时，同样 385 个变量对 6,753 个约束，求解器达到机器精度（残差 1e-16）。**故集合 A 的 0.701 是「红组不是绿组的任何一个子集」，不是「匹配 6,753 个数做不到」。**

---

## S-3a-4. T差 筛选的撤销对 §4 无影响

补丁 S-3a §1 撤销了「与 CD8 T 同步上升的基因不得用于估计 f」这一条。**§4 的两个目标基因集本就未使用该筛选**：

- 集合 A = §2 门槛通过的**全部** 6,753 个基因（门槛只看检出率与阳性细胞数，不看 T差）；
- 集合 B = 点名的趋化因子／驻留标记 ∩ 门槛通过者，其中 `Itga1` 与 `Cd160` **虽被旧的 T差 规则标记，仍在集合 B 内**。

故 **§4 的全部数字（残差 0.701、ESS 152.9、逐基因闭合份额）在撤销后原样成立，无须重算。**
